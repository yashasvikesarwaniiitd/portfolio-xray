# Copy into the Streamlit Community Cloud "Secrets" box (or to .streamlit/secrets.toml
# locally — that file is gitignored). The frontend needs exactly ONE secret: where the
# backend lives. The Anthropic key stays on the backend and is never present here.
BACKEND_URL = "https://portfolio-xray-api.onrender.com"
